## Aplicación de línea de comandos que usa apis de terceros

Recuerda instalar los paquetes de node así
``` npm install  ```
Y recuerda poner tu api key de open weather map en app.js

### Ejemplo

``` node app -d "New York"  ```
